#ifndef ALPHA_TRANSFORM_H
#define ALPHA_TRANSFORM_H

#include "ast.h"

namespace mcc {
    namespace alpha {
        std::vector<knormal::toplevel_t> f(std::vector<knormal::toplevel_t> && ast);
    }
}

#endif
